services:
  watcher:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/data
      - ./config:/app/config   # live-reload config without rebuilding
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
